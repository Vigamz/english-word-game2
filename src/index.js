import React from "react";
import ReactDOM from "react-dom/client";
import EnglishMatchGame from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <EnglishMatchGame />
  </React.StrictMode>
);
